#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    int a=20,b=5,c=-10,d=2,x=12,y=15;
    printf("remove slash in programme's source code  \n");
    
  // printf("1) %d \n",(5*x)+2*((3*b)+4));
      // printf("2) %d \n",(5*(x+2)*3)*(b+4));
    //  printf("3) %d \n ",a==(b=5));
       
      // printf("4) %d \n ",a+=(x+5));//37
       
       //Checks if the values of two operands are equal or not. 
       //If the values are not equal, then the condition becomes true
       
        // printf("5) %d  \n",a!=(c*=(-d)));
        // printf("6) %d \n",a *= c+(x-d));
        
       // printf("7) %d \n",a %= d++);
        //printf("%d ",d);
        
        
        //printf("8) %d\n",a%=++d );
         //printf("%d ",d);
         
       // printf("9)  %d \n ",(x++)*(a+c));
         //utiliser puis incrementer //printf("%d \n",x);
    
  // ! Called Logical NOT Operator. It is used to reverse the logical state of its
  // operand. If a condition is true, then Logical NOT operator will make it false.
  
    //  printf("10) %d \n ",a=x*(b <c)+y*!(b <c));
  // printf("11) %d \n ",!(x-d+c)|| d);
  //! d si d=5 alors !d = 0  
  //printf("12)  %d \n ",a && b || ! 0 && c && ! d);
 // printf("13) %d \n ",((a && b) || (! 0 && c)) && ! d);
     //printf("14) %d ",(a&&b)||(c&&(!d)));
     
     //_________________________________________________________________________________________________
     //_________________________________________________________________________________________________
        // int a=20,b=5,c=-10,d=2,x=12,y=15;
        
      //printf("1) %d \n",5*x+2*3*b+4);
 // printf("2) %d \n",5*x+2*3*b+4);
     // printf("3) %d \n ",a == (b=5));
       
     // printf("4) %d \n ",a+=x+5);//37
       
       //Checks if the values of two operands are equal or not. 
       //If the values are not equal, then the condition becomes true
       
        // printf("5) %d  \n",a!=(c*=(-d)));
        // printf("6) %d \n",a *= c+(x-d));
        
       //printf("7) %d \n",a %= d++);
        //printf("%d ",d);
        
        //printf("8) %d\n",a%=++d );
         //printf("%d ",d);
         
       //  printf("9)  %d \n ",(x++)*(a+c));
         //utiliser puis incrementer //printf("%d \n",x);
    
  // ! Called Logical NOT Operator. It is used to reverse the logical state of its
  // operand. If a condition is true, then Logical NOT operator will make it false.
  
      printf("10) %d \n ",a=x*b <c+y*!b <c);
  
  
  
  
  
  
  
  
  
  system("PAUSE");	
  return 0;
}
