#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
  
  char rep;
  
// continuer ou non 
  /*do {
      printf("vouler vous continuer ?");
      rep = getchar();
      fflush(stdin);
      }while (rep =='o' && rep != 'n');
          
       */   
          
          // ecrire dans l'ecran avec condition . jusqua ecrire space ou taperz entrer 
  
  
   char c;
    while ( putchar(getchar())!= ' ') {
       
    }
    
    
  printf("\n");
  
  
  system("PAUSE");	
  return 0;
}
